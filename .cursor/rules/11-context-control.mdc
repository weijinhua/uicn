---
description: Context control rules
alwaysApply: true
---

# 1️⃣ 最小上下文
- 每次只提供完成任务所需的最少信息
- 默认 ≤ 3 个文件

# 2️⃣ 禁止全量加载
- 禁止加载整个项目 / 目录
- 禁止“analyze entire repo”

# 3️⃣ 优先摘要
- 长文档必须先压缩为摘要再使用
- 禁止直接输入大段原文

# 4️⃣ 只选直接依赖
- 只包含当前任务直接相关代码
- 不包含间接依赖

# 5️⃣ 分步输入
- 大任务拆分为多轮
- 每轮只输入当前步骤所需内容